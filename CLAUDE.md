# CLAUDE.md — PharmaSynth (VR / Meta Quest)

Guidance for Claude Code working in this repository. Read this first each session.

## Project summary
- **PharmaSynth** — a VR chemistry-lab education game for **Meta Quest 3**, built in **Unity 6000.5.2f1** (URP, OpenXR + XRI 3.5.1). Client handoff from a discontinued capstone team — an **audit-and-continue** build, not from-scratch. **Hard deadline: August 31, 2026** (contract).
- **Game concept:** first-person guided lab sim, "PharmaSynth: Gear Up, Synth It Up!". **11 experiments** (Tutorial: Methane; Prelim: Chemical Compounding, Ethyl Alcohol; Midterm: Benzoic Acid, Acetanilide, Acetone, Chloroform; Final: Benzamide, Aspirin, Caffeine, Wine Making). Per-experiment loop: intro cutscene → reagent prep → synthesis → chemical tests → data sheet/quiz → end cutscene (ALWAYS, success or fail variant) → grade screen. Bayesian Knowledge Tracing mastery + rubric grade, **two-part 90% gate** to advance. NPCs: **Pharmee** (robot guide, subtitles + beeps) + **Dr. Jimenez** (examiner, no hints).
- **Master plan:** `C:\Users\MSI\.claude\plans\you-are-the-best-cozy-possum.md` (design spec, architecture, roadmap; **§10 = the confirmed client workflow**). Update it when decisions change.
- **Chemistry authority = manuscript Appendix C** (never the storyboard). Client flags: Benzoic Acid = benzaldehyde + KMnO₄ (manual Exp 4 is a copy-paste defect), Acetanilide acylating agent = acetyl chloride per manual, Benzamide nitrous test = sodium nitrite (manual's "nitrate" is a typo).

## ⭐ SESSION START (first 5 minutes)
1. `git log --oneline -3` on **main** (the user commits checkpoints themselves; `feature/asset-intake` is a dead stub — never use it).
2. Menu **Tools ▸ PharmaSynth ▸ Run Self-Tests** → expect **509/509 ALL GREEN** + zero-error console. (If MCP is down, create `Temp/selftest-autorun-request.txt` — `Editor/SelfTestAutoRun.cs` runs the suite on the next domain reload and writes `Temp/selftest-autorun-result.txt`.)
3. Remaining work lives in ONE tracker: **`Docs/remaining-work-checklist.md`** — check items off there as they land.

## CURRENT STATE (2026-07-09, end of W5.6)
Engine, all 11 experiments (data + auto-built stages), and the **full client-confirmed game loop** are done and regression-locked (385 assertions). A human play-tests by pressing Play (MCP play mode is unreliable).

**The confirmed loop (client-approved 2026-07-09):** player spawns in the front corridor of `SampleScene`. Pharmee guards the **lab door** (the interior door with a window at x≈−2.35, z≈−0.16; the old wall gap is bricked over by `GapSealWall`). Approaching opens his choice panel: **Lab Tour** (door swings open, free roam, nothing graded — poke Pharmee to end the tour) or **Campaign** → explain → episode picker (Tutorial/Prelims/Midterms/Finals, locked periods dimmed) → click the **lab coat** in the locker to don PPE → "I'm ready" → fade + stage loads **armed** (clock frozen) → "the period starts as soon as you walk in" → Proceed → door swings open → **crossing the threshold starts the timer**. Pass → Continue on the grade screen → congrats → fade-teleport to `FrontDoorSpawn` → unlock announcement → loop repeats. Reagent bottles are finite (~125 ml ≈ 1 needed pour + 2 spares); starving a required step → Pharmee offers a period restart (failed attempt recorded). Keyboard dev bypass: **B/1-5/F/R** (`DevExperimentDriver`).

**UI:** screen-locked HUD (`HudRig`, frustum-fitted canvas — Progress top-left, timer top-centre, Settings/Restart/Quit top-right, **Pharmee dialogue bar with icon at the bottom**, visible only while he speaks, 0.5 s fade-out). His overhead bubble (name plate + subtitle) shows only while speaking. `ScreenFader` covers scene loads + teleports. In-lab Settings panel = 4 volume sliders; Quit → MainMenu (dressed spawn room); Restart rebuilds the stage. HUD/bubble/labels outrank transparent geometry via sorting orders (HUD 30000, bubble 29000, world panels 4000–5000, TMP labels 20000).

**Sizes/physics:** all apparatus at real-world scale (`RealSizes` table, 42 prefabs — the ChemLab meshes are metre-accurate; never height-normalise flat tools). `HeadCollisionPushback` on the rig stops phasing through static geometry regardless of movement mode (the simulator's WASD moves the HMD and bypasses the CharacterController by design; thumbstick uses the CC). **Physics pass (2026-07-09):** `PhysicsProfiles` (mass + resting pose per prefab, companion to RealSizes), `GrabPhysicsPolicy` (kinematic-on-shelf → dynamic-on-release), `DropRespawn` (kill-Z + idle return-home) — builder applies all three to spawned props (vessels: profile only, stay kinematic). Any item hosting a Rigidbody MUST have convex MeshColliders (PhysX drops concave ones on dynamic bodies → items fall through the world); `PhysicsProfiles.EnsurePhysics` convexifies + pads degenerate flat-tool colliders. Audit menus: **Tools ▸ PharmaSynth ▸ Physics Audit (Report / Fix Scene Items / Drop Test)** — drop test 42/42 green, report clean.

**Audio:** CC0 clips wired (Pharmee beeps at 0.08–0.10, UI/glass/alarm, menu music, grade stings). AI-generated 2026-07-09 (ElevenLabs via Unity credits, in `Audio/Generated/`): gentle `ambient-lab` hum (vol 0.15), `pour`, `bubble` (boil loop), `burner-ignite`, plus new keys `stir` + `footstep` — all wired in SoundBank, pending a human listen. All 13 action-SFX clips wired AND hooked (footsteps/grab/drops/door/PPE/socket/reaction cues — see checklist §4); sim-loop audio still open. Label style locked: MODERN — Tools ▸ PharmaSynth ▸ Generate Reagent Labels (LabelForge) composites TMP names onto the base + mounts quads on the 16 shelf bottles (re-run after chemical changes). Fume hood: generated model at 2.35 m; matte single-sided material + TopFascia/CornerPanel_L/R primitives hide its broken geometry bands. FootstepPlayer tracks the HEAD (simulator WASD moves the HMD only). Builder also spawns per-station: snap socket + SelectSfx, SimLoopAudio (boil/drip/hiss/shimmer while occupied), TeleportationAnchor floor pad, and a player-only >50°C HotSurface HazardZone at Heat stations. MainMenu is XR-ized (full rig active, world-space canvases incl. ExperimentSelect, XRUIInputModule with mouse kept on).

## Architecture map
All runtime C# under `Assets/PharmaSynth/Scripts/`, global namespace, **thin MonoBehaviours over unit-tested plain-C# cores** (edit-mode verifiable — pattern is mandatory).
- `Experiment/` — `TaskGraph`(+Model), `ExperimentModuleDefinition` (v2 data), `ExperimentRunner` (orchestrator; `StartExperiment` = full start, `PrepareExperiment`/`StartRun` = armed seam for the walk-in timer, `IsArmed`), `MistakeLog`, `QuizBank`+`QuizBankLibrary`.
- `Scoring/` — `MasteryModel` (BKT), `ScoreCalculator`, `ExperimentGrader`.
- `Progression/` — `ProgressionService` (JSON save), `ExperimentCatalog` (roster/periods/prereqs), `ProgressionFlow` (gate queries), `ExperimentLibrary`, `GameFlow` (static SelectedModuleId), `ResultRecorder` (persists every finish — without it unlocks are dead), `UnlockDiff`+`TeleportMath`, `ResultsExport`.
- `Chemistry/` — `LiquidPhysics` (capacity guard BEFORE `LiquidAdded`; `LiquidRejected` event), `LiquidPourer` (35 ml/s), `LiquidTaskBinding` (per-step `requiredMl` accumulation; single-arg `HandleReagent` = full delivery), `ReagentSupplyMonitor`+`ReagentSupplyMath` (starvation → restart prompt), `ReactionRule/Registry`, `ChemicalData`, `TemperatureSim`, `GasCollection`, `CrystallizationController`, `FiltrationController`, `PharmaLiquid.shader`.
- `NPC/` — `GatekeeperModel` (pure door-gate state machine: Blocked→ModeChoice→CampaignExplain→EpisodePick→CoatPrompt→ReadyPrompt→Loading→ThresholdWarn→DoorArmed→Running; +LabTour/+SupplyPrompt/+Debrief→Returning→UnlockAnnounce; **Approach never closes an open door — poke = TalkRequested**), `PharmeeGatekeeper` (thin driver on RobotNPC), `PharmeeBrain` (dialogue reactions, lineSeconds 5.5), `NPCNarrationController` (`BeginLine/EndLine` + `LineStarted/LineEnded` events + auto-hidden `panelRoot`), `PharmeeFace`, `PharmeePoke`, `PharmeeAttitude` (flight lean + hover-wave pulse + talk nod; FaceCamera vertical-forward fallback makes him actually FACE the player), `FloatBob` (bob + Perlin jitter + movable `SetHome`), `PharmeeMover`(+Solver: anchor-follow while running), `CutsceneData/Director/Library` (44 SOs, per-module swap).
- `UI/` — `HudRigController` (ScreenLocked frustum-fit default / LazyFollow fallback) + `HudFollowSolver`, `HudDialogueBar` (portrait+line, fade-out), `ExperimentHudController`, `ChoicePanelController` (reusable option dialog), `LabMenuController` (settings/quit/restart), `ScreenFader`(+`FadeState`), `TabletChecklistController`, `GradeScreenController`, `PostLabController` (quiz + yield stepper), `HubSelectController`, `MainMenuController`, `SettingsService`+`ComfortApplier` (text scale/vignette/snap-turn/subtitle pace listeners), `FaceCamera`, `ProximityLabel` (shelf-aware tag placement), `WristWatchController` (wrist-flip gesture → mini panel + LARGE holo checklist board, rig built on the Left Controller 2026-07-10; watch model still a placeholder cube); `Audio/` — `AudioService` (non-stacking Voice) + `SoundBank` + `PlaySoundOnEnable`.
- `Interaction/` — `ExperimentTaskStation`(+registry), `LabItem`, `PhysicsProfiles`+`GrabPhysicsPolicy`+`DropRespawn` (physics pass), `Mishandling`+`BreakableGlassware`+`SpillMistake` (spill/breakage → Sanitation), `ZoneItemSensor`, `ZoneSimStation` (sim-driven stations, 17/42), `ExperimentSceneBuilder` (+`ExperimentLayout` SO, `SceneAssetLibrary`: spawns each module's stage; `RealSizes` longest-axis normalisation), `ExperimentLauncher` (`LaunchMode {FullStart, PrepareArmed, StageOnly}`; scene loads StageOnly), `DoorOpener` (swinging lab door — colliders stay solid open AND closed), `PlayerTriggerRelay`, `HeadCollisionPushback`, `WaypointBeacon/Guide`, `DevExperimentDriver`, `ExperimentStarter` (legacy).
- `Safety/` — `PPEController` (the locker COAT is the clickable), `FumeHoodZone`, `HazardZone`, `AcidCorrosion`/`SaltBurn`.
- `Editor/` — `PharmaSelfTests` (509 assertions, Tools ▸ PharmaSynth ▸ Run Self-Tests), `PhysicsAudit` (Report/Fix/Drop-Test menus), `SelfTestAutoRun` (MCP-down fallback), `DevCapture` (**Tools ▸ PharmaSynth ▸ Dev Capture**: renders a posed camera to `Temp/dev-capture.png`, pose from `Temp/dev-capture-request.json` — THE visual-verification path; MCP scene capture is broken on this machine).

**Key scene objects (`SampleScene`):** `ExperimentSystems` (runner/launcher/builder/monitor/recorder/devdriver), `HudRig`, `RobotNPC` (brain/narration/gatekeeper/poke/bob/mover + `PharmeeSubtitles` bubble child) at the corridor corner (−1.55, 1.05, 0.55), `LabDoorController` (DoorOpener → `Door (1)`), `DoorApproachTrigger`/`LabThresholdTrigger`/`FrontDoorSpawn` at the door, `DoorChoicePanel`, `GapSealWall` + `ExteriorSeals`, `PPELocker` (hidden host; `LabCoatDisplay` in the wardrobe is the interactable), `ScreenFader`, `Services` (Audio+Settings), `LabTablet`, `PostLabTablet`, `GradeScreen`, `MethaneStage` group + `DynamicStage` (left island), `ReagentShelf` (16 × 0.18 m bottles), `EquipmentShelf` + `BenchApparatus` lineup + `MethaneProps` (back-left counter), `PharmeeAnchors`, `XR Origin (XR Rig)` (CC r0.25 + `HeadCollisionPushback`). `MainMenu`: `MenuCanvas`+`ExperimentSelect`+`SettingsPanel`, `MenuRoom` dressing, `ScreenFader`, `Services`, inactive XR rig scaffold. Build Settings = [MainMenu(0), SampleScene(1)].

## Known gotchas (carry forward)
- `Unity_RunCommand` compiles inside `Unity.AI.*` → fully-qualify/alias `UnityEngine.UI.*` (`UImage`, `UButton`). `System.Reflection`/`ISet`/`GetInstanceID` blocked or error. File writes + `AssetDatabase.DeleteAsset` flagged "requires user interaction" → use Bash for files, load-or-overwrite for assets. **Menu items are exempt** (that's why DevCapture works).
- **Never RunCommand while the user is in Play mode** (`OpenScene` throws; scene edits force-exit play) — check `Unity_ManageEditor GetState` first. One scene per command; `AssetDatabase.Refresh()` before cross-scene asset loads; a `Refresh:true` menu execute can swallow the run in the domain reload (refresh first, then run).
- Edit mode: `OnEnable`/`Awake` don't fire on `AddComponent` → every component needs a public `Bind()`/`SetX()` seam (pattern used throughout). Never `renderer.material` in edit mode (use `sharedMaterial`/MaterialPropertyBlock). `AddComponent<LiquidPhysics>` needs a Renderer host (`CreatePrimitive(Cube)`).
- Before deleting a script, grep ALL of `Assets/` for the type name. FBX imports have no colliders. The ChemLab `_WithLiquid` prefabs had glass/liquid roles reversed (verify mesh names, not GO names); the pack's Glass shader is not URP (materials already ported).
- MCP "named pipe not found" = Unity busy/reloading → wait for `Logs/Editor.log` quiet, retry. MCP "Connection revoked" = AI-seat licensing hiccup (Editor.log shows `Licensing::Client` 404s); it can self-heal after a reload, else the user re-approves under Project Settings → AI → Unity MCP — meanwhile use the `SelfTestAutoRun` request-file fallback. `Unity_Camera_Capture` fails on every camera here → use DevCapture.
- Transparent geometry (curtains/glass) doesn't z-write → text/canvas visibility is decided by **sortingOrder** (values in the UI section above).
- The suite's `[ExperimentLauncher] Unknown moduleId: does-not-exist` warning is a deliberate negative test, not a defect.
- Poppler/PDF: TEMP is hijacked → the Read tool cannot open PDFs; use `"C:/Program Files/Git/mingw64/bin/pdftotext.exe"` or python pypdf. The manuscript exports via gdoc `export?format=txt`.
- Internet works via `curl` (kenney.nl fine; github-raw 429). CC0 sources: Kenney, OpenGameArt, Poly Haven/ambientCG, Quaternius.

## Remaining work
**→ `Docs/remaining-work-checklist.md` is the single tracker** (consolidated 2026-07-09): per-experiment test-vessel wiring, physics/resting-pose pass, art/audio production, menu XR-ization, settings listeners, results screen, on-device Quest week, client sign-offs. `Docs/client-signoff-request.md`, `Docs/asset-production-spec.md`, `Docs/on-device-test-plan.md` unblock the gated buckets.

## Environment
- Windows 11; PowerShell primary, Git Bash available. Unity 6000.5.2f1, URP 17.5, Input System 1.19, XRI 3.5.1, OpenXR 1.17.1 + meta-openxr. **No Cinemachine — never animate the XR camera** (fades + staging only). Active build target = Android (IL2CPP/ARM64/ASTC, Meta Quest feature + Touch profiles configured); Standalone keeps OpenXR with InitManagerOnStart=FALSE (headless-PC-safe).
- **Unity MCP** = the official Assistant server (needs the AI seat; approve under Project Settings → AI). MCP tools cost no credits; **`Unity_AssetGeneration_*` and Assistant chat DO — always flag before spending.**
- Git: work on **main**; commit only when asked; no destructive git ops. Handoff backups live off-repo at `C:\Users\MSI\PharmaSynth-handoff-backup\`; raw DCC sources in `Docs/raw-art-sources/` (gitignored; labcoat source there).

## Testing & verification
Quest 3 not delivered yet — test in-editor (XR Device Simulator; WASD moves the HMD, thumbstick moves the body). Every change: run the self-test suite (must stay green and grow) + zero-error console + a DevCapture screenshot for visual work. Escalate to the client if no headset by W5 (Aug 4–10).

## Docs index (`Docs/`)
`project-overview.md` (orientation) · `gaps.md` (gap analysis) · `mechanics-guide.md` (how each mechanic works/extends) · `build-and-run.md` (run/test/build) · `gameplay-rules.md` (designer rulebook) · `content-authoring.md` (add content without engine changes) · **`remaining-work-checklist.md` (THE tracker)** · `client-signoff-request.md` · `asset-production-spec.md` · `on-device-test-plan.md` · `audit-report.md` (historical W1 audit).

## Working conventions
- Build/verify through Unity MCP; DevCapture screenshots confirm visual changes; experiments are DATA (ScriptableObjects), not scenes; match inherited code style where reused.
- **ART IS IN SCOPE to create/replace/improve** (user directive): DCC authoring, re-texturing, Asset Store, or Unity AI generation (credits — flag first). Storyboard/cutscene PDFs are references to EXCEED; never copy their garbled labels or chemistry.
- Confirm game-design changes with the user; the plan file records decisions.
