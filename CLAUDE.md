# CLAUDE.md — PharmaSynth (VR / Meta Quest)

Guidance for Claude Code working in this repository. Read this first each session.

## Project summary
- **PharmaSynth** — a VR chemistry-lab education game for **Meta Quest 3** (confirmed), built in **Unity 6000.5.2f1** (URP).
- Client handoff from a discontinued capstone team. **NOT a from-scratch rebuild**: the previous team's full Unity project (38 scripts, 49 prefabs, assembled lab scene, IntegrationGuide.md) survives inside `Docs/handoff_assets/Transition.unitypackage` — this is an **audit-and-continue** build.
- **Game concept (confirmed):** first-person guided lab sim, "PharmaSynth: Gear Up, Synth It Up!". 11 experiments (Tutorial: Methane; Prelim: Chemical Compounding, Ethyl Alcohol; Midterm: Benzoic Acid, Acetanilide, Acetone, Chloroform; Final: Benzamide, Aspirin, Caffeine, Wine Making). Per-experiment loop: intro cutscene → reagent prep → synthesis → chemical tests → data sheet/quiz → success/fail cutscene → grade screen. Bayesian Knowledge Tracing mastery, **90% gate** to advance. NPCs: Pharmee (robot guide, subtitles + beeps) + Dr. Jimenez (human examiner, no hints).
- **Hard deadline: August 31, 2026** (contract). Tier build order: Tier 1 = Tutorial + Prelim + Benzoic Acid + Aspirin; Tier 2 = rest of Midterm + Benzamide + Wine; Tier 3 = Caffeine.

## THE PLAN (approved 2026-07-07)
`C:\Users\MSI\.claude\plans\you-are-the-best-cozy-possum.md` — the critique-hardened master plan: full design spec, architecture, asset gap list, week-by-week roadmap, error-handling matrix, Quest 3 perf budget, verification strategy. Follow it; update it when decisions change.
Key user requirements not in the old docs: wrist-flip watch checklist (right hand default, button fallback); auto-checking task conditions; end cutscene ALWAYS (success or fail variant); NPC robot dialogues; improved animations; **spacious lab layout** (wide clearances around tables/gather points, unobstructed experimentation).

## ⭐ SESSION HANDOFF — mid-Week 5 (2026-07-08). READ THIS FIRST.

**Fresh session — first 5 minutes:** (1) `git log --oneline -3` on **main** — the user commits checkpoints themselves (recent: `f1bfdb8 "fixed assets"`). (2) Menu **Tools ▸ PharmaSynth ▸ Run Self-Tests** → expect **ALL GREEN** + zero-error console. (3) Play `SampleScene` directly for the lab (launcher auto-starts the GameFlow selection, default Methane; keyboard B/1-5/F/R) or Play `MainMenu` and click Tutorial/Laboratory (desktop mouse only — no XR rig in the menu scene yet).

**Where we are:** W1–W4 DONE; **W5 in progress, W6 content pulled forward (2026-07-08)**. The entire game **engine is feature-complete and verified** (**157/157** assertions via menu **Tools ▸ PharmaSynth ▸ Run Self-Tests**), and a **playable Methane tutorial** is assembled in `Assets/Scenes/SampleScene.unity`. Console is zero-error. Work branch: **`main`** (all W1–W5 work committed there by the user; `feature/asset-intake` is a **dead stub at the planning commit — do not use it**). Commit only when asked.

**W5 progress so far:**
- **Tier-1 content COMPLETE (all 5 authored as v2 data):** Tutorial_Methane, Prelim_ChemicalCompounding, Prelim_EthylAlcohol, **Midterm_BenzoicAcid** (NEW — 9 tasks), **Final_Aspirin** (NEW — 7 tasks, overheat branch). Self-tests now load+solve every module to 100% respecting prerequisites, all phases complete, mastery/score spines build. **Chemistry note flagged for client:** Benzoic Acid = **benzaldehyde + 0.1% KMnO4 oxidation** (per Appendix C reagent list: benzaldehyde/0.1% KMnO4/6N HCl/propyl alcohol), which supersedes the plan's tentative "toluene + KMnO4".
- **ALL remaining content authored (2026-07-08):** the full **11-experiment roster now exists as v2 data** (added Acetanilide, Acetone, Chloroform, Benzamide, Caffeine, WineMaking) — see the architecture-map "Experiments authored" line. This is effectively W6's content workstream done early.
- **Collision + interactivity pass DONE (2026-07-08, user request):** all **118/118 Environment furniture meshes** now have static MeshColliders (+92 this pass; tables/cabinets/shelves/windows/counters all solid). All **42/42 `ChemLabEquipment` prefabs** grab-enabled = convex/box collider + Rigidbody(gravity) + `XRGrabInteractable` (liquid-fill meshes skipped; 5 skinned tools got box colliders from bounds). **Consequence to watch:** these prefabs are now gravity-on physics objects — when composing experiment scenes, rest them on a collidered surface and keep concurrent active rigidbodies within the Quest budget (≤40, kinematic-on-grab). Convex hulls are crude for tall apparatus (tripod/stand) — fine for grab, refine in the art pass.
- **Hands-on interaction foundation (roadmap step 1) STARTED:** `Interaction/LabItem.cs` (prop identity) + `ExperimentTaskStation.requiredItemId`/`AcceptsItem()` so a **trigger station accepts only the correct grabbable prop**. Methane's 5 poke-cubes converted to **item-matched trigger zones**; 5 grabbable props (`XRGrabInteractable`+`Rigidbody`+`LabItem`, gravity-off stand-ins) created under **`MethaneProps`** on a `ReagentBench`: `reagent-jar`→PrepareMixture, `glass-tube`→SetupApparatus, `burner`→HeatMixture, `collection-tube`→CollectGas, `lit-splint`→TestGas. Structurally verified 5/5 stations + 5/5 props. *(The primitive stand-ins + `ReagentBench` from this step were later **superseded** by the play-test fix pass below: real prefab props on the right-wall counter, stations as zone pads on the right island.)*

- **Play-test fix pass DONE (2026-07-08, from user screenshots):** (1) **glass rendering** — the pack's `ChemLab/Glass` shader was never URP-ported (error-magenta); its 5 materials (`GlassMat`, `GlassInnerMat`, `AmberGlassInnerMat`, `TransparentLiquidMat`, `RedLiquidMat`) are now URP Lit transparent, and the 5 `_WithLiquid` prefabs had **glass/liquid roles reversed** — fixed: root mesh=glass, `Liquid` child=PharmaLiquid fill, `LiquidPhysics.mainRenderer`=liquid child (+ `SendMeshBounds` now prefers the mainRenderer's mesh). (2) **Reagents display in the `3x4` open shelf grid** (left wall, x≈−5.2) — 16 vessels raycast-seated into compartments. (3) **Real apparatus props** replace primitives: Vial_Brown_WithLabel/TestTube/BunsenBurner/GraduatedCylinder_50mL/GlassRod seated on the right-wall counter (x≈3.95) with billboarded name tags; note they are true-to-scale (small). (4) **Stations = thin zone pads** (0.3×0.012×0.3, trigger rises 0.48 m) grounded on the right island (x=1.15, z −2.5…−4.5, surface y 0.91) + BEGIN button (chunkier, at z −2.1); **labels detached to `WorldLabels`** root. (5) **PPELocker** sign at the locker room (0.35, 1.15, 1.35), clear of meshes. (6) **`UI/FaceCamera.cs` billboard** on all labels/tags + LabHUD/LabTablet/PharmeeSubtitles/GradeScreen — text never mirrors. (7) HUD `BarFill` was a non-Filled Image (ignored fillAmount) → Filled/Horizontal/Left stretched in `BarBg`; HUD text auto-sizes; Pharmee bubble raised to y 1.95. (8) `LiquidTestVessel` (W2 shader demo — the floating blue cylinder) deleted. (9) Rig **CharacterController radius 0.1→0.25** (walls/doors/tables all had colliders; thin radius made collision feel absent; note: simulator WASD moves the HMD itself and legitimately bypasses collision — thumbstick locomotion collides).

**How to play-test right now:** open SampleScene → press Play. **Hands-on path:** grab a prop off the **right-wall counter (x≈3.95)** — real prefabs with billboarded name tags — and carry it into its matching station zone pad on the right island → completes that step (order enforced; wrong prop ignored, wrong order → WrongStep). **Keyboard fallback:** DevExperimentDriver on `ExperimentSystems` — **B**=begin, **1-5**=complete step, **F**=finish, **R**=retry. MCP-driven play mode is unreliable on this PC — a human presses Play. **⏳ NEEDS: human grab-test in the XR Device Simulator (zone sizes/heights), sockets/respawn so dropped props return, clean reagent-label textures.**

**Architecture map** (all runtime C# under `Assets/PharmaSynth/Scripts/`, global namespace, thin MonoBehaviours over unit-tested plain-C# cores):
- `Experiment/` — `TaskGraph`(+Model), `ExperimentModuleDefinition` (v2 data: graphTasks/phases/skills/rubric/threshold), `ExperimentRunner` (orchestrator + events + two-part gate), `MistakeLog` (9 LabErrorTypes), `ExperimentFlowManager` (legacy, one Ethyl module).
- `Scoring/` — `MasteryModel` (BKT), `ScoreCalculator` (rubric, normalized), `ExperimentGrader` (run-state→grade).
- `Progression/` — `ProgressionService` (versioned JSON save + gate/unlock), `ExperimentCatalog` (ordered 11-experiment roster + periods + prereq chain — one source of truth for menu/hub/select), `ProgressionFlow` (unlocked/next/period-door/overall-completion queries over the service), `ExperimentLibrary` (SO: runtime-safe refs to all 11 module assets — `ExperimentLibrary.asset`), `GameFlow` (static cross-scene SelectedModuleId).
- `Chemistry/` — `LiquidPhysics` (event-driven, de-hardcoded), `LiquidTaskBinding` (context-aware reagent→task + fume-hood check), `ReactionRule/Registry`, `ChemicalData`, `TemperatureSim`, `GasCollection`, `CrystallizationController`, `FiltrationController`, `PharmaLiquid.shader`.
- `NPC/` — `PharmeeBrain` (dialogue state machine), `PharmeeFace` (IPharmeeFace), `NPCNarrationController` (subtitles + Say()), `CutsceneData`+`CutsceneDirector` (VR-safe, end cutscene always), `ModuleCutsceneController` (inherited, PlayableDirector).
- `UI/` — `ExperimentHudController`, `TabletChecklistController`, `GradeScreenController`, `WristWatchController`, `MainMenuController` (Tutorial/Laboratory/Settings/Quit + `ResolveLabTarget`), `FaceCamera` (Y-axis billboard for world labels/panels).
- `Interaction/` — `ExperimentTaskStation`(+registry, +item-matched trigger via `requiredItemId`/`AcceptsItem`), `LabItem` (grabbable-prop identity), `ExperimentLauncher` (swaps runner to any module by id from the library → menu/hub drives this), `ExperimentStarter`, `WaypointGuide`, `DevExperimentDriver`.
- `Safety/` — `PPEController`, `FumeHoodZone`, `HazardZone`, `AcidCorrosion`/`SaltBurn` (inherited).
- `Editor/PharmaSelfTests.cs` — the **157-assertion regression suite**, 13 sub-suites (TaskGraph/Mastery/Score/Grader/Runner/Progression/ChemVisual/UI/W4-rigs/Interaction/ProgressionFlow/Library/Content — Content loads + solves all 11 modules to 100%). Run via Tools ▸ PharmaSynth ▸ Run Self-Tests; expect ALL GREEN + zero-error console.
- **Experiments authored — ALL 11 now exist as v2 data** (`ScriptableObjects/Experiments/`): `Tutorial_Methane`(5), `Prelim_ChemicalCompounding`(6), `Prelim_EthylAlcohol`(7), `Midterm_BenzoicAcid`(9), `Final_Aspirin`(7), `Midterm_Acetanilide`(10), `Midterm_Acetone`(10), `Midterm_Chloroform`(10), `Final_Benzamide`(9), `Final_Caffeine`(9), `Final_WineMaking`(8). Cutscenes in `ScriptableObjects/Cutscenes/` (Methane only so far — each experiment still needs its 4 CutsceneData SOs). **Client chemistry flags:** Benzoic Acid = benzaldehyde oxidation (not toluene); Acetanilide acylating agent = acetyl chloride per manual (acetic anhydride is safer alt); Benzamide nitrous-acid test = sodium nitrite (manual's "nitrate" is a typo). Wine uses the standard 6-category rubric for now — its bespoke workmanship/appearance/flavour rubric is a follow-up.
- **Scene objects (SampleScene = lab):** `ExperimentSystems` (runner+launcher+starter+guide+devdriver; runner.startOnAwake=OFF, `ExperimentLauncher` drives it via `GameFlow.SelectedModuleId`, library=`ExperimentLibrary.asset`), `LabHUD`, `LabTablet`, `GradeScreen`, `RobotNPC` (at −3.1,0.7,−3.0; subtitle bubble above head), `MethaneStations` (5 **zone pads** on the right island x=1.15, z −2.5…−4.5) + `MethaneProps` (5 **real prefab props** with `LabItem` ids on the right-wall counter x≈3.95), `ReagentShelf` (16 filled vessels **inside the `3x4` wall grid**, x≈−5.2), `WorldLabels` (all detached billboarded labels + prop tags), `BeginButton` (island front, z −2.1), `PPELocker` (locker room, 0.35,1.15,1.35), `WaypointMarker` (small emissive pin), `XR Origin (XR Rig)` (CC radius 0.25).
- **`Assets/Scenes/MainMenu.unity`** (NEW): world-space canvas (title + Tutorial/Enter Laboratory/Settings/Quit) → `MainMenuController` (loads SampleScene, sets GameFlow selection via `ProgressionFlow.NextExperiment`). Build Settings = [MainMenu(0), SampleScene(1)]. **VR follow-up:** menu scene uses a plain camera — add an XR Origin + `TrackedDeviceGraphicRaycaster` (controller-ray clicks) for on-headset use.
- **Reagents:** 16 `ChemicalData` assets in `ScriptableObjects/Chemicals/` (Chem_*), each with name/state/liquidColor/hazard/fume-hood/bp. `ReagentShelf` stocks them as colour-tinted `_WithLiquid` vessels (kinematic display, still grabbable; LiquidPhysics colours them at runtime, MaterialPropertyBlock previews in edit mode). Clean reagent-LABEL textures are still an art-pass TODO.

**ART ASSETS = IN SCOPE to CREATE/REPLACE/IMPROVE (user directive 2026-07-08).** Nothing is blocked on "sourcing." Produce the best outcome by any means: author in Blender/DCC (`Docs/raw-art-sources/` has the lab-coat + test-tube sources), improve/re-texture the imported packs, buy/import Asset Store where it's faster, or use **`Unity_AssetGeneration_*` (COSTS Unity AI credits — flag before spending)** for textures/props. Storyboard/cutscene PDFs are references to EXCEED, not copy (their labels are garbled AI text). Re-author clean reagent-label textures. Placeholder poke-cubes/primitive UI are stand-ins to be replaced with real props/polished canvases.

## REMAINING IMPLEMENTATION ROADMAP (W5 → Aug 31 deadline)

Engine is done; remaining work = **content (fast, data-driven)** + **art/interaction (needs assets, now in scope)** + **on-device**. Suggested order:
1. **Real interactions** — carry-prop-to-zone loop ✅ DONE for Methane (real prefab props + `requiredItemId` stations). NEXT: upgrade zones to real verbs — pour→`LiquidTaskBinding`, place-on-scale→`WeighingScaleController`, burner→`TemperatureSim`, gas→`GasCollection`, ice-bath→`CrystallizationController`, Büchner→`FiltrationController` — plus XRI sockets + drop-respawn. The logic layer already works & is tested.
2. **Art pass 1 — Pharmee & UI:** re-point `PharmeeFace` at the robot's screen mesh; author Pharmee animations (idle-float/talk/gesture/celebrate/warn) + face-state materials; polish the world-space HUD/tablet/watch/grade canvases (replace primitive panels); proper wrist-watch model + gesture tune.
3. **Audio:** create SFX (pour/bubble/shatter/alarm/UI) + Pharmee robotic beep "voice" set + ambient lab loop + menu music; add an `AudioService` (mixer groups) and wire the AudioSource hooks already present (NPCNarrationController, BreakableGlassware).
4. **PPE flow polish:** locker/ante-room, mirror/avatar reflection showing worn PPE, glove material swap on hands.
5. **Dr. Jimenez:** create/rig a scientist examiner (assessment mode: observes, no hints); `ExaminerNPC` component to build.
6. **Cutscenes polish:** the data-driven director works; add staging/fades/props; wine-making "7 days & 7 nights" time-skip; per-experiment cutscene data.
7. **Content — remaining experiments as v2 data** ✅ **DONE (2026-07-08): all 11 authored.** Still TODO per experiment: reaction rules (ReactionRegistry) + per-vessel LiquidTaskBinding + grabbable-prop station wiring (like Methane) + the 4 CutsceneData SOs each. The DATA (task graphs/phases/skills/rubric) is complete and self-test-verified.
8. **Menus & flow:** MainMenu scene ✅ DONE (desktop-clickable; Build Settings = [MainMenu(0), SampleScene(1)]). Still TODO: menu XR rig + `TrackedDeviceGraphicRaycaster` for headset use, Settings panel content (audio/text/comfort/handedness), Period hub (3 mastery-gated doors), experiment-select UI, post-lab quiz UI + data-sheet yield entry.
9. **On-device (when Quest 3 arrives):** finish Android OpenXR feature toggles if not already; comfort + 90 Hz perf pass (ASTC/atlasing/draw-calls); wrist-gesture ergonomics; full revision-checklist UAT; ISO/IEC 25010 acceptance instrument; final APK + turnover docs.

**Known gotchas (carry forward):**
- `Unity_RunCommand` compiles inside `Unity.AI.*` namespace → `Image` etc. resolve to `Unity.AI.Image`; **fully-qualify `UnityEngine.UI.Image`** (alias `using UImage = UnityEngine.UI.Image;`). `System.Reflection`/`ISet` are blocked; filesystem writes (`File.WriteAllText`, `Directory.Delete`, bulk `AssetDatabase` delete/move loops) get flagged "requires user interaction" — do those via the Bash tool or single `AssetDatabase.CreateAsset` calls.
- **Edit-mode:** `OnEnable`/`Awake` don't fire on `AddComponent` → give components a public `SetX()`/bind method for edit-mode tests (pattern used throughout).
- MCP "named pipe not found" = Unity busy/reloading; wait for `Logs/Editor.log` quiet, retry. XR/package ops have crashed the editor twice (auto-recovered).
- Before deleting a script, **grep all of `Assets/` for the TYPE NAME** (not just scan scene/prefab components) — the LiquidData deletion broke XRBottleUI.
- **Never call `renderer.material` in edit mode** (leaks material instances — Unity errors). Use `sharedMaterial` or a `MaterialPropertyBlock` for edit-mode tinting; runtime scripts (LiquidPhysics) may use `.material` freely.
- **Pack prefab naming traps:** in the ChemLab `_WithLiquid` prefabs the child named "Liquid" was the liquid MESH but wore the glass material while the root glass mesh wore the liquid material — verify mesh names (`MeshFilter.sharedMesh.name`), not GameObject names. The pack's `ChemLab/Glass` shader is NOT URP-compatible (renders magenta) — its materials are now URP Lit transparent.
- FBX imports have **no colliders**; add them in-scene (walkable floor = `Floor (1)`). Standalone XR `InitManagerOnStart`=FALSE (headless-PC-safe); Android=TRUE.
- Poppler/PDF: machine TEMP hijacked `C:\Program Files\poppler-24.08.0\...`; use `pdftotext.exe` at Git path + python pypdf; manuscript via gdoc `export?format=txt`.

## Detailed progress log (history; the SESSION HANDOFF section above is the current-state summary)
### Week 1 setup DONE
- ✅ All Docs processed via 13-agent digest → `Docs/digests/doc-digest-13agents.json` (+ plan critique in `plan-critique-3agents.json`; storyboard/cutscene page renders in `Docs/digests/images/`, gitignored).
- ✅ Plan approved: `C:\Users\MSI\.claude\plans\you-are-the-best-cozy-possum.md`. Work on branch **`feature/asset-intake`**.
- ✅ Handoff archives backed up (MD5-verified) to `C:\Users\MSI\PharmaSynth-handoff-backup\` (also holds `pre-import-snapshot/`). All zips + `Transition.unitypackage` now DELETED from repo (backups verified). Only the 2 reference PDFs remain in `Docs/handoff_assets/`.
- ✅ Assets organized into `Assets/PharmaSynth/Art/{Environment,Equipment,Characters,UI}`; raw DCC sources in `Docs/raw-art-sources/` (gitignored).
- ✅ Previous team's project imported & merged (URP-converted; 0 compile errors; 0 missing scripts across scene + 91 prefabs). Their 36 scripts live at `Assets/Scripts/` (+ `IntegrationGuide.md`); the assembled lab is `Assets/Scenes/SampleScene.unity` with Pharmee/RobotNPC already staged.
- ✅ **Script audit** → `Docs/audit-report.md`. Verdict: GO/reuse the framework. 7 reuse / 18 refactor / 4 rewrite / 7 discard. 5 critical items folded into W2 (missing liquid shader, thin reaction model, naive wrong-reagent detect, dead Tunneling.cs→use XRI vignette, cutscene double-fire bug).
- ✅ **XR set up**: `com.unity.cloud.gltfast` + `com.unity.xr.meta-openxr` 2.5.0 (pulls AR Foundation 6.5). XRI Starter Assets + XR Device Simulator samples imported. XR Origin rig (teleport/snap+continuous turn/dynamic+grab move/climb) placed in SampleScene at (0,0,0.8) facing the lab; Device Simulator added. **OpenXR is the sole active loader for Android** (bootstrapped `Assets/XR/XRGeneralSettingsPerBuildTarget.asset`). Play mode runs with ZERO runtime errors.
- ⏳ **XR follow-up (manual, ~2 min in UI):** Project Settings → XR Plug-in Management → OpenXR (Android): enable **Meta Quest** feature group + an interaction profile (Oculus Touch) + **Foveated Rendering**, and switch active build target to Android. Programmatic `OpenXRSettings.GetSettingsForBuildTargetGroup` returned null via MCP, so do this in the UI.
- ⚠️ **Layout finding (spacious-lab requirement):** room is 10.6×11.2 m but the two central islands (`Table_1`, `Table_1 (1)`) sit only **1.2 m apart** — snug for VR. Widen the central aisle to ≥1.5 m during the W1-2 blockout; front of room (Z −1.8→+3.0) is ~4.8 m clear (good). Bench tops at Y≈1.0 m (good VR height); `Table_1*` are tall 1.8 m units (shelf-topped islands).
- ⚠️ **Unity crashed once** during the meta-openxr/AR-Foundation package resolve (crash dump in `%LOCALAPPDATA%/Temp/Unity/Editor/Crashes/`); auto-recovered, no data loss. If MCP returns "named pipe not found", Unity is busy/restarting — wait for `Logs/Editor.log` to go quiet, then retry.
- ✅ **W2 batch 1 (2026-07-08):** 29 keepers migrated to `Assets/PharmaSynth/Scripts/{Experiment,Chemistry,Interaction,NPC,UI,Safety}` (GUID-preserving; 0 missing refs); 8 dead files deleted (7 discards + Tunneling.cs); **`PharmaSynth/Liquid` URP shader authored** (`Art/Shaders/PharmaLiquid.shader` + `Materials/PharmaLiquid_Default.mat`) — fulfills LiquidPhysics's full property contract, stereo-instancing-safe, scene-color approximated as transparency, visually verified (LiquidTestVessel in scene = living demo); ModuleCutsceneController rewritten (single-fire finish, director.stopped + Hold-safe duration check, skip stops coroutine); **XRI TunnelingVignette** attached to rig camera, wired to move/snap-turn/continuous-turn providers; **central island aisle widened 1.2m→1.74m** (islands at x=±1.56; outer clearance 1.40m).
- ⚠️ **MCP play-mode gotchas:** (1) issuing `Unity_RunCommand` while in play mode force-exits play; (2) play mode also exits silently shortly after MCP-initiated entry (no console/log errors — cause undiagnosed, possibly AI-assistant bridge or focus behavior). Assume MCP-driven play tests are unreliable; stage state in edit mode + captures, and have a human press Play for runtime validation. Standalone XR `InitManagerOnStart` deliberately set FALSE (OpenXR init on a headset-less PC also kills play); Android keeps auto-init ON for Quest builds. A stale `com.unity.xr.oculus` loader request was purged; OpenXR is now assigned for BOTH targets.
- ✅ **W2 hygiene (2026-07-08):** deleted `LiquidData.cs` broke `XRBottleUI` (CS0246 → Safe Mode); fixed by repointing XRBottleUI at `LiquidPhysics`/`ChemicalData`. **Lesson: before deleting a script, grep the whole `Assets/` for the TYPE NAME, not just scan scene/prefab components.** Removed deprecated **`com.unity.xr.oculus`** (unused; was causing the stale Oculus loader errors + XR-mgmt Oculus entry). Imported **TMP Essential Resources**. Enabled **OculusTouch + MetaQuestTouchPlus + HandInteraction** profiles on **Standalone** OpenXR (clears the Palm Pose / Hand Interaction Poses validation issues). ChemLabEquipment "Deprecated" badge is just the Asset Store listing — imported assets are fine, no action.
- ✅ **Android/Quest build config DONE (2026-07-08):** Android Build Support installed (user, via Hub). **Active build target switched to Android** (full reimport, no crash). Android OpenXR settings sub-asset now exists; enabled: **MetaQuestFeature (Meta Quest Support), MetaQuestTouchPlus + OculusTouch controller profiles, HandInteractionProfile, FoveatedRenderingFeature**. Android XR loader = OpenXRLoader (sole), InitManagerOnStart=TRUE. Player: Vulkan-first + OpenGLES3, IL2CPP, ARM64, **ASTC** textures. Standalone keeps OpenXR loader w/ InitManagerOnStart=FALSE (headless-PC-safe for Editor/Simulator). All 3 Project Validation issues cleared; console zero-error.
  - **Note:** active target is now **Android** — Editor play + XR Device Simulator still work on Android target, but iteration reimports are Android-format. If Editor iteration feels slow, can switch back to Windows for dev and to Android only for device builds (both XR-configured).
- ✅ **W2 batch 2 — scoring/progression spine (2026-07-08):** built as plain unit-testable C# (no MonoBehaviour deps) in global namespace to match inherited style. **TaskGraph** (`Scripts/Experiment/TaskGraph.cs` + `TaskGraphModel.cs`): 4 phases (ReagentPrep/Synthesis/ChemicalTests/DataSheet), prerequisite gating (BlockedByPrerequisite = wrong-step-order signal), weighted Progress01, PhaseCompleted/AllRequiredCompleted events, pluggable `RegisterCondition(id, Func<bool>)` + `Tick()` world-state auto-check. **MasteryModel** (`Scripts/Scoring/MasteryModel.cs`): standard BKT per LabSkill, `Observe(skill,correct)`, OverallMastery, `IsMastered(threshold)`. **ScoreCalculator** (`Scripts/Scoring/ScoreCalculator.cs`): RubricWeights auto-normalized (fixes manual's inconsistent weights) → GradeBreakdown 0..100, plus TimeSubScore. **ExperimentModuleDefinition extended additively** (graphTasks, masteryThreshold=0.90, BktParameters, trackedSkills, RubricWeights, parTimeSeconds + BuildTaskGraph/BuildMasteryModel/BuildScoreCalculator helpers) — legacy `tasks`/`ExperimentTaskDefinition` kept so ExperimentFlowManager still compiles. **Verified via 26-assertion Unity_RunCommand self-test (all PASS): prereq blocking, weighted progress 0.25→0.75→1.0, phase events, auto-check, BKT convergence past 0.90 gate + slip drop, weight normalization.** Console zero-error.
- ✅ **W2 batch 2 — error matrix + grading pipeline (2026-07-08):** **MistakeLog** (`Scripts/Experiment/MistakeLog.cs`): `LabErrorType` 9-type matrix (WrongReagent/WrongStep/DroppedGlassware/Overheat/FireSafety/MissingPPE/FumeHoodViolation/ChemicalContact/HazardousAction), Count/CountOf/CountOfAny, MistakeRecorded event, `CategoryFor()` mapping errors→rubric criteria. **ExperimentGrader** (`Scripts/Scoring/ExperimentGrader.cs` + GradingConfig): run-state (TaskGraph completion + MistakeLog + elapsed/par + quiz) → per-category sub-scores → `GradeBreakdown` via ScoreCalculator; procedural/safety/sanitation mistakes penalize their categories. **ExperimentFlowManager enhanced additively** (MistakeCount + Mistakes MistakeLog; Mark* methods now log typed errors) — all existing signatures/callers unchanged (verified caller map: ProcedureChecklistUI event, LiquidPhysics CompleteTask×3, ExperimentTaskTrigger, ExperimentErrorReporter). **Verified via 18-assertion self-test: mistake counting/attribution, perfect run=100%, realistic run w/ 4 mistakes+slow time+partial quiz = exactly 81.08% (hand-checked), partial completion=45% w/ correct per-category values.** Console zero-error.
- ✅ **W2 batch 2 — ExperimentRunner orchestrator (2026-07-08):** `Scripts/Experiment/ExperimentRunner.cs` (MonoBehaviour) owns TaskGraph+MasteryModel+MistakeLog+ExperimentGrader built from module v2 data. Fires `ExperimentStarted/TaskCompleted/PhaseCompleted/ProgressChanged/MistakeRecorded/ExperimentFinished` (+ inspector UnityEvents) for HUD/tablet/watch/NPC/grade-screen. Auto-wires the error matrix: out-of-order CompleteTask → WrongStep mistake. **Two-part gate** in `ExperimentResult`: passed = (grade.Total ≥ threshold%) AND (BKT mastery ≥ masteryThreshold). `Update()` does AdvanceTime + graph.Tick (auto-check); `AdvanceTime` public for deterministic tests; `Finish` idempotent; `Retry` rebuilds. Added `MasteryModel.IsTracked` + `MistakeLog.SkillFor`. **Verified via 26-assertion edit-mode self-test: single clean pass = 96% grade (grade gate ✓) but 0.674 mastery (mastery gate ✗ → not passed) — correct BKT pedagogy. 70 total assertions now green across the whole scoring engine, all without play mode.**
- **Scoring engine is feature-complete** (`Scripts/Experiment/{TaskGraphModel,TaskGraph,MistakeLog,ExperimentRunner}.cs` + `Scripts/Scoring/{MasteryModel,ScoreCalculator,ExperimentGrader}.cs`). Legacy `ExperimentFlowManager` still drives the inherited Ethyl module until migrated.
- ✅ **W2 COMPLETE (2026-07-08).** Remaining batch-2 items all done + verified (console zero-error throughout):
  - **ProgressionService** (`Scripts/Progression/`): versioned JSON save (persistentDataPath + `.bak` backup, corruption-recovery via backup), per-module best grade/mastery/passed/attempts, `IsPassed`/`IsUnlocked(prereq)`. 17-assertion self-test (logic + JSON round-trip; disk IO is verify-by-inspection since sandbox blocks File writes in RunCommand).
  - **UI controllers** (`Scripts/UI/`): `ExperimentHudController` (title, HH:MM:SS timer, progress bar that dips on mistakes, task toasts), `TabletChecklistController` (phase-grouped ☑/▶/☐ checklist from graphTasks + reaction footer), `GradeScreenController` (grade%/mistakes/time/per-criteria/PASSED-TRY AGAIN, Continue gated on pass, Retry/Continue UnityEvents), `WristWatchController` (supination + gaze gesture w/ hysteresis + InputAction button fallback, compact step/progress/mastery). All subscribe to ExperimentRunner events. 24-assertion self-test on the formatters/builders.
  - **Chemistry model extended**: ChemicalData +state/boilingPoint/precipitateColor/pH/evolvesGas/HazardType/requiresFumeHood; ReactionRule +minTemperatureC/ReactionOutcome/evolvesGas/expectedObservation +TemperatureSatisfied(); ReactionRegistry null-guarded. **LiquidPhysics de-hardcoded**: ripped out fermentation logic + ExperimentFlowManager coupling → fires `LiquidAdded`/`ReactionOccurred`/`WrongReagentMixed` events. **`LiquidTaskBinding`** = context-aware reagent→task (correct reagent completes its step w/ prereq order enforced; unexpected reagent = WrongReagent; out-of-order = WrongStep). 9-assertion self-test + scene integrity (0 missing scripts).
  - **Tutorial: Methane** authored as v2 module: `Assets/PharmaSynth/ScriptableObjects/Experiments/Tutorial_Methane.asset` (5 steps ×20%, phases/skills/rubric/ILOs, threshold 0.90, par 300s) — loads + builds a runnable graph.
  - **PharmaLiquid material + LiquidPhysics applied to all 5 ChemLab `_WithLiquid` prefabs** (Beaker 100/500, Erlenmeyer 400, GradCyl 50, TestTube) via PrefabUtility.
  - **Scoring/UI engine totals ~120 passing edit-mode assertions**, zero reliance on play mode.
- ✅ **Floor fall-through FIXED (2026-07-08):** Laboratory pack meshes imported with 0 colliders → CharacterController gravity dropped the player through. Added 26 MeshColliders to structural objects (floor/walls/tables/cabinets/etc.) + TeleportationArea on `Floor (1)` (10.5×11.1 m, top Y≈0.2); rig spawn raised to Y=0.22. Continuous-move + teleport now both work. **Gotcha: FBX models need colliders added in-scene (import has none); the walkable floor mesh is `Floor (1)`, and there's also an empty parent `Floor` container.**
- ✅ **W3 code systems (2026-07-08):** **PharmeeBrain** (`Scripts/NPC/`): NPC state machine (Idle/Greeting/Instructing/Warning/Celebrating/Encouraging) + face expression, subscribes to ExperimentRunner — greets on start, instructs current step from task hints, warns w/ warning-face per error type, celebrates/encourages mirroring the gate. `SetRunner()` for runtime bind (edit-mode-safe subscribe). Added `NPCNarrationController.Say()` reactive single-line API (audit gap). **TemperatureSim** (`Scripts/Chemistry/`): exact exponential HeatModel (timestep-independent, unit-tested) + target/overheat threshold events + AtLeast() auto-check predicate (powers distillation cut-offs + aspirin overheat). **GasCollection**: fill-fraction, balloon scale, full event, Collected() predicate. All plain-core + thin MonoBehaviour. **~50 more passing assertions (PharmeeBrain 8, Temp/Gas 17, binding 9, UI 24, progression 17...).**
  - **Engine wired into scene:** `ExperimentSystems` GameObject → ExperimentRunner bound to Tutorial_Methane; PharmeeBrain on RobotNPC wired to runner. (RobotNPC has no NPCNarrationController yet — add w/ subtitle UI in scene-assembly.)
  - **Fixed XRI affordance NRE spam:** disabled 12 broken AffordanceSystem components (Poke/NearFar hover-tint receivers with unset theme — known Starter Assets issue) on the controllers. Console zero-error.
- ✅ **W3 COMPLETE (2026-07-08) — playable Methane tutorial assembled in SampleScene.** Interaction components (`Scripts/Interaction/`, `Scripts/Safety/`): `ExperimentTaskStation` (+registry; auto-hooks XRI selectEntered → CompleteTask), `ExperimentStarter` (poke→Begin/Retry), `WaypointGuide` (marker → current step's station), `PPEController` (poke→DonPPE gates lab door), `PharmeeFace` (IPharmeeFace material tint). **World-space UI built + wired to ExperimentSystems runner:** `LabHUD` (title/timer/progress-bar/toast), `LabTablet` (phase-grouped checklist + reaction footer), `GradeScreen` (grade%/mistakes/time/breakdown/PASSED-TRYAGAIN, Retry→starter.Begin, Continue gated on pass, auto-shows on ExperimentFinished), `PharmeeSubtitles` (Pharmee bubble + NPCNarrationController + skip) → PharmeeBrain wired to narration+face. **5 Methane task stations** (poke-cubes, labeled, order-enforced) + Begin button + PPE locker + waypoint marker, all wired. Verified structurally (10/10) + visually (scene capture shows full HUD/tablet/stations/NPC). **Re-runnable regression suite:** `Tools ▸ PharmaSynth ▸ Run Self-Tests` (`Scripts/Editor/PharmaSelfTests.cs`) = 31/31 green (TaskGraph/BKT/Score/Grader/Runner/Progression/Chem/UI). Console zero-error.
  - **Gotchas found:** (1) inside Unity_RunCommand the compilation namespace is `Unity.AI.*`, so `Image` resolves to `Unity.AI.Image` — fully-qualify `UnityEngine.UI.Image` (alias `using UImage = UnityEngine.UI.Image;`). (2) OnEnable/Awake don't fire on AddComponent in edit mode — give components a public `SetRunner()`-style bind for edit-mode tests. (3) faceRenderer on PharmeeFace defaulted to `Ears_Black_Matt_0` — **user: re-point at the robot's screen-face mesh in Inspector.**
- ✅ **W4 COMPLETE (2026-07-08).** **DevExperimentDriver** (`Scripts/Interaction/`) on ExperimentSystems: keyboard **B**=begin, **1-5**=complete step, **F**=finish, **R**=retry — drives the whole loop in-editor Play mode (unblocks testing without XR interaction). **Cutscene system** (`Scripts/NPC/`): `CutsceneData` SO (subtitle beats + Pharmee expression, VR-safe, no camera anim) + `CutsceneDirector` (subscribes runner: Intro on start, ReagentPrep on phase-complete, Success/Failure on finish — **end cutscene ALWAYS plays**). 4 Methane cutscenes authored (`ScriptableObjects/Cutscenes/`); PharmeeBrain `deferIntroToDirector=true` + director.onCutsceneFinished→brain.InstructCurrent handoff (no subtitle conflict). **Chem-rigs:** `CrystallizationController` (timed liquid→crystal, done event/predicate), `FiltrationController` (accumulate filtrate, done/predicate). **Hazard matrix completed:** `FumeHoodZone` (occupancy + Contains) + `HazardZone` (contact→RecordMistake, debounced); LiquidTaskBinding now flags `requiresFumeHood` reagents used outside the hood as FumeHoodViolation. **2 more Tier-1 experiments authored as v2 data:** `Prelim_ChemicalCompounding` (6 tasks), `Prelim_EthylAlcohol` (7 tasks) — 3 experiments total on the template. **Regression suite now 45/45 green** (added cutscene/cryst/filt/hazard/content suites). Console zero-error.
- **Engine + tutorial are feature-complete and headless-verified.** Runtime scripts across `Scripts/{Experiment,Chemistry,Scoring,Progression,UI,NPC,Interaction,Safety,Editor}`. To play-test: open SampleScene, press Play, use keyboard B/1-5/F/R (or poke the stations in the simulator).
- ⏳ NEXT (W5+ — art/asset + on-device, needs headset + user): replace abstract poke-stations with real reagent/apparatus grab interactions wired to the built LiquidTaskBinding/TemperatureSim/GasCollection/Crystallization/Filtration systems; Pharmee skeletal animations + re-point PharmeeFace at the screen mesh; source Dr. Jimenez rigged model; real audio (SFX + Pharmee beeps) via an AudioService; PPE mirror/avatar reflection; tune UI world positions/scale in-headset; author remaining experiments (M1 Benzoic Acid, M2-M4, F1-F4) as v2 data; on-device Quest QA + 90 Hz perf pass. **All W2-W4 engine systems are built & tested — remaining work is content authoring (data, fast) + art/interaction wiring (needs assets) + on-device validation.**

## Environment
- OS: Windows 11. Shell: PowerShell primary; Git Bash also available.
- Unity `6000.5.2f1`, URP 17.5, Input System 1.19, XRI 3.5.1, OpenXR 1.17.1, Timeline. **No Cinemachine (deliberate — never animate the XR camera; cutscenes = PlayableDirector staging + fades).**
- XR: add `com.unity.xr.meta-openxr` for Quest FFR (plain OpenXR lacks it); Meta XR All-in-One SDK (Asset Store, account-gated) optional later. Verify exactly ONE active XR loader.
- Git: work on **`main`** (all history lives there; the user commits checkpoints themselves). `feature/asset-intake` is a dead stub at the planning commit — ignore it. Handoff binaries + digest images are gitignored (backup is off-repo). Commit only when asked.
- **Gotcha:** the machine's TEMP points into `C:\Program Files\poppler-24.08.0\Library\bin` — poppler is broken, so the Read tool CANNOT read PDFs. Use `"C:/Program Files/Git/mingw64/bin/pdftotext.exe"` for text and python (pypdf + pillow installed) for page/image extraction. Manuscript Google Doc exports cleanly via `curl -sL "<doc-url>/export?format=txt"`.

## Unity MCP — connection setup & gotchas (IMPORTANT)
Uses Unity's **official** Assistant MCP Server (`com.unity.ai.assistant`), not a community bridge.
- Requires a **Unity AI subscription seat** — without it every call returns "Connection revoked". Fix: assign seat in Unity dashboard → fully restart Unity.
- Approval: **Project Settings → AI → Unity MCP Server** (client `claude-code` Accepted; all 52 tools safe to enable).
- **Credits:** MCP tools (scene edits, scripts, console) consume NO Unity AI credits; only `Unity_AssetGeneration_*` / AI Assistant chat do. Always flag before spending credits.
- **Gotcha:** during long imports/package resolves the editor stops answering ("Unity not detected / no fresh discovery files"). Wait for Editor.log to go quiet (~30s inactivity) and retry.

## Testing
Quest 3 headset NOT delivered yet. Test in-editor via **XR Device Simulator**; escalate to client if no device by W5 (Aug 4-10). Wrist-gesture ergonomics + comfort + 90 Hz validation are day-1 on-device items. Verify via MCP captures (`Camera_Capture`, `SceneView_CaptureMultiAngleSceneView`) + `Unity_ReadConsole` zero-error gate.

## Project documentation (Docs/ — authored 2026-07-08, repo-verified)
- **`Docs/project-overview.md`** — standalone orientation for a new engineer/client: concept, game loop, roster, architecture map (all 64 scripts), content pipeline, play-testing, Quest build config, verification strategy.
- **`Docs/gaps.md`** — honest gap analysis vs plan/contract: 17-row system status matrix, 11-experiment wiring matrix, 22 defects/risks with W5–W8 fix windows, 9 client decision items, order of attack. **Adversarially fact-checked against the repo.**
- **`Docs/mechanics-guide.md`** — how every mechanic works + how to extend it (task graph, scoring/BKT, mistakes, interactions, chemistry sims, safety, NPC/cutscenes, progression, UI).
- **`Docs/build-and-run.md`** — run/test/build: in-editor iteration, simulator caveats, regression suite, Build Settings, Quest 3 Android build, perf budget, workstation gotchas.
- **`Docs/gameplay-rules.md`** — designer/client-facing rulebook: player journey, 11-experiment table, progression + grading rules, penalty matrix, mastery in plain words, safety rules, assists.
- **`Docs/content-authoring.md`** — cookbook for adding content without engine changes: module authoring (worked example + 5-step checklist), reagents/reactions, the Methane scene-wiring pattern, cutscenes, chemistry accuracy rules, enum index appendix.
- **`Docs/remaining-work-checklist.md`** — ⭐ the actionable checkbox tracker for ALL remaining work: per-experiment wiring matrix, missing assets, built-but-unplaced components, attribute passes, scenes/UI, NPC, on-device, client decisions. **Check items off here as they land.**
- **`Docs/audit-report.md`** — the W1 inherited-code audit (historical).

## Docs handoff (all processed — see digests)
- `Docs/Documentations/gdocs_link_for_the_manuscript` → manuscript (capstone doc; Appendix C = the WCC lab manual with per-experiment procedures, weights, rubric).
- `Docs/Documentations/implementation_plan.pdf` → OUR signed contract (P1–P11 phases, Aug 31 deadline, deliverables).
- `Docs/handoff_assets/` → storyboard + cutscenes PDFs (reference only; we exceed their quality; their labels are garbled and some pages have copy-paste chemistry errors — never copy chemistry from storyboard, use manual/Appendix C).
- Chemistry conflicts already reconciled in the plan (§3.3); flagged for client: acetanilide acylating agent, scoring weights.

## Working conventions
- Build/verify through Unity MCP; capture screenshots to confirm visual changes.
- C# to `Assets/PharmaSynth/Scripts/...`; experiments are DATA (ScriptableObjects), not scenes; match inherited code style where reused.
- No destructive git ops; commit/push only when asked.
- Confirm game-design changes with the user; the plan file records all decisions.
